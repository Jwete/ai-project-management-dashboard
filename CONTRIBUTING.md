# Collaboration Guidelines

Welcome to the team! To maintain a secure environment, please follow these rules:
1. Never commit code directly to the `main` branch.
2. Create a new branch for every feature (`feature/your-feature-name`).
3. Open a Pull Request (PR) and request a review from a teammate before merging.
4. Ensure all code outputs align with our Responsible AI framework.
